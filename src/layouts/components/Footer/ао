.footer {
 
 
 
 

 
  gap: 12px;
  margin-top: auto; /* Притискає футер до низу, якщо контенту мало */
}

.text {
  font-size: 0.95rem;
  color: var(--text-secondary);
  text-align: center;
  line-height: 1.6;
  max-width: 700px; /* Обмежуємо ширину, щоб текст гарно виглядав на десктопі */
  margin: 0;
  /* Важливо для мобілок: дозволяємо переносу тексту */
  word-wrap: break-word;
  overflow-wrap: break-word;
}

.text strong {
  color: var(--brand-color);
  font-weight: 800;
  letter-spacing: 0.5px;
}

/* Адаптація для мобільних пристроїв */
@media (max-width: 480px) {
  .footer {
    padding: 30px 15px;
    gap: 10px;
  }

  .text {
    font-size: 0.85rem; /* Трохи зменшуємо шрифт на маленьких екранах */
    width: 100%;
  }
}